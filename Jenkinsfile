pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                gradlew build
            }
        }
        stage('Run on Device') {
            steps {
                echo 'will run in th future..'
            }
        }
    }
}
