pipeline {
    agent any

    stages {
        stage('Build') {
            steps {
                build 'jenkins-android-test'
            }
        }
        stage('Run on Device') {
            steps {
                echo 'will run in th future..'
            }
        }
    }
}
